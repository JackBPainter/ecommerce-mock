import React from "react"

const Image = () => {

    return (
        <div className={`${className} image-container`}>
            <img className="image-grid" src={} />
        </div>
    )
}

export default Image